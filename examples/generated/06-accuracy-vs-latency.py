# Generated by GraphUnslopify from a FigureSpec.
# tool version 0.3.0, spec sha256:865b39eeea84e764
# Every number below is a presentation decision owned by the generator.
# Change the spec and regenerate rather than editing this file.
#
# The spec is embedded in the exported files' metadata, so any reader can
# recover exactly what produced this figure. DISCLOSURE below is ready to
# paste into a methods section.
import matplotlib

matplotlib.use("Agg")

import matplotlib.pyplot as plt
import pandas as pd

RC_PARAMS = {
    "font.size": 9,
    "axes.labelsize": 9,
    "axes.titlesize": 9,
    "xtick.labelsize": 8,
    "ytick.labelsize": 8,
    "legend.fontsize": 8,
    "axes.linewidth": 0.8,
    "xtick.major.width": 0.8,
    "ytick.major.width": 0.8,
}

FIGSIZE = (3.4, 2.6)
PALETTE = ["#0072B2", "#D55E00", "#009E73", "#CC79A7", "#E69F00", "#56B4E9", "#8C564B", "#000000"]
RECEDED_ALPHA = 0.4
SECOND_CHANNEL_THRESHOLD = 2
MARKERS = ["o", "s", "^", "D", "v", "P", "X", "*"]

DATA_PATH = "data/benchmarks.csv"
X_FIELD = "latency_ms"
Y_FIELD = "accuracy"
GROUP = "model"
SERIES_ORDER = ["baseline", "+augment", "+distill", "ours"]
EMPHASIS = "ours"
STEM = "06-accuracy-vs-latency"
FORMATS = ["png", "svg", "pdf"]
DPI = 600

TOOL = "GraphUnslopify 0.3.0"
SPEC_HASH = "865b39eeea84e764"
SPEC_JSON = "{\"title\":\"Accuracy against cost\",\"group\":\"model\",\"series_order\":[\"baseline\",\"+augment\",\"+distill\",\"ours\"],\"emphasis\":{\"series\":\"ours\"},\"legend\":{\"show\":true,\"position\":\"lower_right\"},\"output\":{\"size\":\"single_column\",\"formats\":[\"png\",\"svg\",\"pdf\"],\"dpi\":600,\"stem\":\"06-accuracy-vs-latency\"},\"style\":{\"preset\":\"paper\",\"grid\":false,\"despine\":true},\"data\":{\"path\":\"data/benchmarks.csv\",\"format\":\"csv\",\"columns\":[\"model\",\"dataset\",\"accuracy\",\"latency_ms\",\"params_m\"]},\"x\":{\"field\":\"latency_ms\",\"label\":\"Latency\",\"unit\":\"ms\",\"scale\":\"linear\"},\"y\":{\"field\":\"accuracy\",\"label\":\"Top-1 accuracy\",\"unit\":\"%\",\"scale\":\"linear\"},\"kind\":\"scatter\",\"trendline\":\"none\"}"
DISCLOSURE = (
    f"This figure was produced by {TOOL} from a declarative figure "
    f"specification (sha256:{SPEC_HASH}); the plotting code was generated, "
    f"the underlying data were not."
)


def load():
    return pd.read_csv(DATA_PATH)


def series_names(frame):
    if SERIES_ORDER is not None:
        return list(SERIES_ORDER)
    return sorted(frame[GROUP].dropna().unique().tolist(), key=str)


def series_style(name, index, total):
    colour = PALETTE[index % len(PALETTE)]
    # Colour alone stops working past a handful of series, so add a second channel.
    channel = MARKERS[index % len(MARKERS)] if total > SECOND_CHANNEL_THRESHOLD else "o"
    if name == EMPHASIS:
        # The series being argued for reads as the plain form, not a dashed one.
        channel = "o"
    if EMPHASIS is None:
        style = {"colour": colour, "size": 18, "alpha": 1.0, "zorder": 2}
    elif name == EMPHASIS:
        style = {"colour": colour, "size": 30, "alpha": 1.0, "zorder": 3}
    else:
        style = {"colour": colour, "size": 12, "alpha": RECEDED_ALPHA, "zorder": 1}
    style["marker"] = channel
    return style


def draw(ax, df):
    names = series_names(df)
    for index, name in enumerate(names):
        frame = df[df[GROUP] == name]
        style = series_style(name, index, len(names))
        ax.scatter(
            frame[X_FIELD],
            frame[Y_FIELD],
            label=str(name),
            color=style["colour"],
            s=style["size"],
            alpha=style["alpha"],
            zorder=style["zorder"],
            marker=style["marker"],
        )


def finish(fig, ax):
    ax.set_xlabel("Latency (ms)")
    ax.set_ylabel("Top-1 accuracy (%)")
    ax.set_title("Accuracy against cost")
    ax.grid(False)
    for side in ("top", "right"):
        ax.spines[side].set_visible(False)
    ax.legend(
        title=None,
        frameon=False,
        loc="lower right",
    )
    fig.tight_layout()


def metadata_for(fmt):
    # Embedding the spec means a reviewer can recover what produced the figure.
    if fmt == "png":
        return {"Software": TOOL, "Description": SPEC_JSON}
    if fmt == "svg":
        return {"Creator": TOOL, "Description": SPEC_JSON}
    return {"Creator": TOOL, "Subject": SPEC_JSON}


def check(fig):
    # Optional. Install graphunslopify for readability and colour checks.
    try:
        from graphunslopify import inspect_figure
    except ImportError:
        print("graphunslopify not installed, skipping figure checks")
        return True
    report = inspect_figure(fig, target_width_in=3.4)
    print(report)
    return report.ok


def main():
    df = load()
    with plt.rc_context(RC_PARAMS):
        fig, ax = plt.subplots(figsize=FIGSIZE)
        draw(ax, df)
        finish(fig, ax)
        check(fig)
        for fmt in FORMATS:
            path = f"{STEM}.{fmt}"
            kwargs = {"bbox_inches": "tight", "metadata": metadata_for(fmt)}
            if fmt == "png":
                kwargs["dpi"] = DPI
            fig.savefig(path, **kwargs)
            print(f"wrote {path}")
    plt.close(fig)
    print(DISCLOSURE)


if __name__ == "__main__":
    main()
