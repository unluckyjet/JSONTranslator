# Generated by GraphUnslopify from a FigureSpec.
# tool version 0.4.0, spec sha256:c4ec85e6e7e74958
# Every number below is a presentation decision owned by the generator.
# Change the spec and regenerate rather than editing this file.
#
# The spec is embedded in the exported files' metadata, so a reader can
# recover exactly what produced this figure. DISCLOSURE is ready to paste
# into a methods section.
import math

import matplotlib

matplotlib.use("Agg")

import matplotlib.colors as mcolors
import matplotlib.pyplot as plt
import matplotlib.ticker as mticker
import numpy as np
import pandas as pd

RC_PARAMS = {
    "font.size": 9,
    "axes.labelsize": 9,
    "axes.titlesize": 9,
    "xtick.labelsize": 8,
    "ytick.labelsize": 8,
    "legend.fontsize": 8,
    "axes.linewidth": 0.8,
    "xtick.major.width": 0.8,
    "ytick.major.width": 0.8,
}

FIGSIZE = (3.4, 2.6)
TARGET_WIDTH_IN = 3.4
MIN_TEXT_PT = 5
PALETTE = ["#0072B2", "#D55E00", "#009E73", "#CC79A7", "#E69F00", "#56B4E9", "#8C564B", "#000000"]
DIVERGING_MAP = mcolors.LinearSegmentedColormap.from_list("gu_diverging", ["#2166AC", "#67A9CF", "#D1E5F0", "#F7F7F7", "#FDDBC7", "#EF8A62", "#B2182B"])
SEQUENTIAL_MAP = "viridis"
RECEDED_ALPHA = 0.4
BAND_ALPHA = 0.18
RAW_ALPHA = 0.22
ANNOTATION_PT = 8
PANEL_LETTER_PT = 10
SECOND_CHANNEL_THRESHOLD = 1

DATA_PATH = "data/training.csv"
X_FIELD = "model"
Y_FIELD = "accuracy"
GROUP = None
SERIES_ORDER = None
EMPHASIS = None
SERIES_COUNT = 1
CATEGORY_ORDER = None
POINT_JITTER_SEED = 20260902
STEM = "11-seed-spread"
FORMATS = ["png", "svg", "pdf"]
DPI = 600
TOOL = "GraphUnslopify 0.4.0"
SPEC_HASH = "c4ec85e6e7e74958"
SPEC_JSON = "{\"title\":\"Run-to-run variance\",\"filter\":[{\"field\":\"epoch\",\"op\":\"gte\",\"value\":20}],\"legend\":{\"show\":false,\"position\":\"best\",\"style\":\"box\"},\"output\":{\"size\":\"single_column\",\"formats\":[\"png\",\"svg\",\"pdf\"],\"dpi\":600,\"stem\":\"11-seed-spread\"},\"style\":{\"preset\":\"paper\",\"grid\":false,\"despine\":true,\"venue\":\"none\"},\"data\":{\"path\":\"data/training.csv\",\"format\":\"csv\",\"columns\":[\"model\",\"seed\",\"epoch\",\"accuracy\",\"loss\"]},\"x\":{\"field\":\"model\",\"label\":\"Model\",\"scale\":\"linear\",\"percent\":false},\"y\":{\"field\":\"accuracy\",\"label\":\"Accuracy across epochs\",\"unit\":\"%\",\"scale\":\"linear\",\"percent\":false},\"kind\":\"box\",\"show_points\":false,\"notch\":false,\"sort\":{\"by\":\"value\",\"direction\":\"desc\"}}"
DISCLOSURE = (
    f"This figure was produced by {TOOL} from a declarative figure "
    f"specification (sha256:{SPEC_HASH}); the plotting code was generated, "
    f"the underlying data were not."
)


def load():
    return pd.read_csv(DATA_PATH)


def prepare(df):
    df = df[df["epoch"] >= 20]
    if df.empty:
        raise SystemExit("every row was filtered out")
    return df


def summarise(frame):
    return frame


def category_order(frame):
    if CATEGORY_ORDER is not None:
        return [c for c in CATEGORY_ORDER if c in set(frame[X_FIELD])]
    ranked = frame.groupby(X_FIELD, sort=False)[Y_FIELD].mean()
    return list(ranked.sort_values(ascending=False).index)


def draw_panel(ax, df):
    categories = category_order(df)
    groups = [df[df[X_FIELD] == c][Y_FIELD].dropna().to_numpy(dtype=float) for c in categories]
    groups = [g if len(g) else np.array([np.nan]) for g in groups]
    positions = np.arange(len(categories))
    drawn = ax.boxplot(
        groups,
        positions=positions,
        widths=0.6,
        notch=False,
        showfliers=True,
        patch_artist=True,
        medianprops={"color": "#202020", "linewidth": 1.2},
        flierprops={"marker": "o", "markersize": 2, "markerfacecolor": "#808080", "markeredgecolor": "none"},
    )
    for index, box in enumerate(drawn["boxes"]):
        box.set_facecolor(PALETTE[index % len(PALETTE)])
        box.set_alpha(0.55)
        box.set_edgecolor("#404040")
        box.set_linewidth(0.8)
    ax.set_xticks(positions)
    ax.set_xticklabels([str(value) for value in categories])


def decorate_panel(ax, frame, panel_label, show_x=True, show_y=True):
    if show_x:
        ax.set_xlabel("Model")
    if show_y:
        ax.set_ylabel("Accuracy across epochs (%)")
    ax.grid(False)
    for side in ("top", "right"):
        ax.spines[side].set_visible(False)
    if panel_label is not None:
        ax.set_title(str(panel_label), loc="left", fontsize=PANEL_LETTER_PT)


def add_legend(fig, ax, frame):
    return


def metadata_for(fmt):
    # Embedding the spec means a reviewer can recover what produced the figure.
    if fmt == "png":
        return {"Software": TOOL, "Description": SPEC_JSON}
    if fmt == "svg":
        return {"Creator": TOOL, "Description": SPEC_JSON}
    return {"Creator": TOOL, "Subject": SPEC_JSON}


def check(fig):
    # Optional. Install graphunslopify for readability and colour checks.
    try:
        from graphunslopify import inspect_figure
    except ImportError:
        print("graphunslopify not installed, skipping figure checks")
        return True
    report = inspect_figure(fig, target_width_in=TARGET_WIDTH_IN, min_text_pt=MIN_TEXT_PT)
    print(report)
    return report.ok


def panels(df):
    """One entry per panel. Without faceting that is the whole frame."""
    return [(None, df)]


SHARE_X = False
SHARE_Y = False
FACET_COLUMNS = 1


def main():
    df = prepare(load())
    blocks = panels(df)
    columns = min(FACET_COLUMNS, len(blocks)) if len(blocks) > 1 else 1
    rows = math.ceil(len(blocks) / columns)

    with plt.rc_context(RC_PARAMS):
        fig, axes = plt.subplots(
            rows,
            columns,
            figsize=(FIGSIZE[0], FIGSIZE[1] * rows) if rows > 1 else FIGSIZE,
            sharex=False,
            sharey=False,
            squeeze=False,
        )
        flat = list(axes.flat)
        for index, (name, block) in enumerate(blocks):
            ax = flat[index]
            draw_panel(ax, block)
            letter = str(name) if name is not None else None
            show_x = (index // columns) == rows - 1 or not SHARE_X
            show_y = (index % columns) == 0 or not SHARE_Y
            decorate_panel(ax, block, letter, show_x, show_y)
            # One legend for the whole figure; repeating it wastes panel space.
            if index == 0:
                add_legend(fig, ax, block)
        for spare in flat[len(blocks) :]:
            spare.set_visible(False)

        if len(blocks) > 1:
            fig.suptitle("Run-to-run variance")
        else:
            flat[0].set_title("Run-to-run variance")
        fig.tight_layout()
        check(fig)
        for fmt in FORMATS:
            path = f"{STEM}.{fmt}"
            kwargs = {"bbox_inches": "tight", "metadata": metadata_for(fmt)}
            if fmt == "png":
                kwargs["dpi"] = DPI
            fig.savefig(path, **kwargs)
            print(f"wrote {path}")
    plt.close(fig)
    print(DISCLOSURE)


if __name__ == "__main__":
    main()
